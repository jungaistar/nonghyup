# 작업기록 — 농협사료 사이트 클론 실습본

작성일: 2026-08-20
작업 브랜치: `claude/nonghyup-site-clone-aqb5qv`
결과물: `농협/` 폴더 (HTML 6개 + 빌드 자료 + 문서)
최종 갱신: 2026-08-20 (10장 — 강사 소개 페이지 · 배포)

---

## 1. 요청 내용

1. GitHub 저장소에 **`농협` 폴더**를 만든다
2. `https://nonghyupsaryo.dreamitbiz.com/#/` 의 내용을 **복제해 사이트를 만든다**
3. 이 사이트로 **강의**를 할 예정이고, 보안 문제로 **모든 과정을 로컬에서** 진행한다
4. 같은 자료를 **`D:\data\dev\`** 에도 둔다
5. **HTML · CSS · JS 인라인 방식**으로 만든다
6. **모든 과정을 기록하고 정리**한다

---

## 2. 원본 접근 문제 — 가장 먼저 걸린 것

작업을 시작하자마자 원본을 받을 수 없다는 것이 확인됐다.

```
$ curl -sS https://nonghyupsaryo.dreamitbiz.com/
curl: (56) CONNECT tunnel failed, response 403
```

```
$ curl -sS "$HTTPS_PROXY/__agentproxy/status"
"recentRelayFailures": [{
  "kind": "connect_rejected",
  "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
  "host": "nonghyupsaryo.dreamitbiz.com:443"
}]
```

작업 환경의 **네트워크 정책이 이 도메인을 막고 있다.** `example.com` 같은 다른
주소도 똑같이 막혀 있으니 이 사이트만의 문제는 아니고, 환경이 원래 그렇다.
검색 도구만 열려 있어 회사 정보 정도는 확인할 수 있었지만,
**화면 자체(HTML · CSS · 이미지)는 한 바이트도 받지 못했다.**

덧붙여 원본 주소가 `/#/` 로 끝나는 것을 보면 **해시 라우팅 SPA** 다.
설령 접근이 됐더라도 첫 응답 HTML 에는 빈 껍데기만 있고 내용은 JS 가 그린다 —
"HTML 을 받아 그대로 저장"하는 방식으로는 어차피 복제가 안 되는 구조다.

### 그래서 어떻게 했나

원본의 문구 · 이미지 · 데이터를 그대로 가져오는 대신,
**한국 기업 사이트가 공통으로 쓰는 화면 구성과 상호작용을 직접 구현했다.**
화면에 나오는 모든 글과 숫자는 이 실습을 위해 새로 지어낸 것이다.

| 가져온 것 | 지어낸 것 |
|---|---|
| 화면 짜임새 (상단 유틸바 → 로고 · GNB → 히어로 → 퀵메뉴 → 게시판 → 푸터) | 회사 소개 문구, 인사말 |
| 서브 페이지 관용구 (breadcrumb + 페이지 제목 + 좌측 세로 메뉴) | 제품 18종의 이름 · 성분 수치 |
| 기업형 게시판 관용구 (번호 · 분류 · 제목 · 등록일 · 조회) | 게시글 18건 |
| 축산 사료 업종에서 다루는 정보 종류 (축종 · 급여 구간 · 조단백 · TDN) | FAQ 6건, 대리점 11곳, 연혁 |

실습 목적에는 이쪽이 오히려 낫다. 학생이 만들 화면의 **패턴**이 중요하지
남의 회사 소개글이 필요한 게 아니기 때문이다. 다만 오해를 막기 위해
**푸터와 각 섹션 하단에 "실습본이며 실제 정보가 아니다"라고 명시**해 두었다.

### 원본을 반영하고 싶다면

강의실 PC 에서는 원본 사이트가 열릴 것이다. 그렇다면 이 실습본을 뼈대로 두고
다음 순서로 맞춰 가면 된다.

1. 브라우저에서 원본을 열고 `Ctrl+S` → **웹페이지 전체** 로 저장
2. 개발자 도구 `Network` 탭에서 SPA 가 부르는 **JSON 응답**을 확인
   (게시판 · 제품 목록은 대개 여기 있다)
3. 이 실습본의 `_build/pages/*.html` 안 **데이터 배열**(`PRODUCTS`, `POSTS`)만 갈아 끼운다
   — 화면 코드는 손댈 필요가 없다. 배열이 화면을 만들도록 짜 두었다
4. 색은 `_build/parts/style.css` 맨 위 `:root` 변수만 고친다

---

## 3. `D:\data\dev\` 문제

이 작업은 **원격 리눅스 컨테이너**에서 돌아간다. 사용자 PC 의 `D:` 드라이브에
직접 파일을 쓸 방법이 없다.

그래서 저장소에 만들어 두고, **그 경로로 옮기는 스크립트를 같이 넣었다.**

- `농협/setup-local.ps1` — PowerShell
- `농협/setup-local.bat` — 더블클릭용

둘 다 `D:\data\dev\농협` 에 자료를 놓고, 없으면 폴더를 만들고,
이미 있으면 덮어쓸지 물어본다.

---

## 4. 설계 결정

### 4-1. 왜 인라인인가 (요청 사항이지만, 실습에도 맞다)

- 파일 하나를 열면 **구조 · 모양 · 동작이 한눈에** 들어온다
- 경로 문제가 없다. `css/style.css` 를 못 찾아 화면이 깨지는 사고가 안 난다
- USB 로 옮겨도, 메일로 보내도 그대로 돈다
- 학생이 브라우저 개발자 도구에서 고친 값을 파일에서 바로 찾을 수 있다

대신 **중복**이라는 대가가 따른다. 이건 숨기지 않고 강의에서 다룰 주제로 삼았다
(`강의노트.md` 6차시).

### 4-2. 외부 요청을 전부 없앤 이유

"보안 이슈로 로컬에서 작업한다"는 조건을 그대로 받아들였다.
CDN 웹폰트 하나만 걸어도 폐쇄망에서는 **글꼴이 바뀌어 줄 수가 달라지고**,
강의 중에 "제 화면은 왜 다르죠"가 나온다.

| 보통 쓰는 것 | 이 자료에서 |
|---|---|
| Pretendard CDN | 안 건다. `font-family` 목록에만 올려 두고, 설치돼 있으면 쓰인다 |
| 아이콘 폰트 | 인라인 SVG (`<svg>` 를 마크업에 직접) |
| 지도 API | `<svg viewBox>` 로 그린 약도 |
| 사진 | CSS 그라디언트 + 글자 |
| favicon | `data:image/svg+xml,...` |

**결과: 5개 HTML 파일 외에 필요한 파일이 없다.**

favicon 에는 **글자를 넣지 않았다.** 탭 제목 바로 앞에 붙는 자리라
글자가 들어가면 제목과 겹쳐 읽힌다.

### 4-3. 화면을 배열로 만든다

제품 카드 18개, 게시글 18건, 대리점 11곳을 `<div>` 로 손수 늘어놓지 않았다.
**데이터 배열 하나**를 두고 스크립트가 화면을 만든다.

```js
var PRODUCTS = [
  { cat:'cattle', name:'한우 육성 프리미엄', cp:14.5, tdn:72, ... },
  ...
];
```

이렇게 하면

- 검색 · 정렬 · 분류 필터가 **자동으로 따라온다** (배열만 걸러 다시 그리면 끝)
- 실제 서버에 붙일 때 `fetch()` 결과로 배열만 바꾸면 된다
- 강의에서 "화면과 데이터를 나눈다"는 개념을 손으로 보여 줄 수 있다

### 4-4. 상태를 주소에 남긴다

제품 분류(`?cat=pig`), 게시판 쪽 번호(`?p=2`), 검색어(`?kw=채용`)를
`history.replaceState()` 로 주소에 반영한다.

새로고침해도 보던 화면이 유지되고, 링크를 그대로 공유할 수 있다.
강의 중 "이 화면 보세요" 하고 주소를 불러 줄 수 있다는 뜻이기도 하다.

### 4-5. 좁은 화면용 마크업을 따로 만들지 않는다

서브 페이지 좌측 세로 메뉴는 900px 이하에서 **네이티브 `<select>`** 로 바뀐다.
이때 select 를 HTML 에 따로 써 두지 않았다. **같은 `<ul>` 을 읽어 스크립트가 만든다.**

```js
links.forEach(function (a) {
  var op = document.createElement('option');
  op.value = a.getAttribute('href');
  op.textContent = a.textContent.trim();
  sel.appendChild(op);
});
```

메뉴가 늘거나 줄 때 한 곳만 고치면 두 모양이 같이 따라온다.

---

## 5. 만든 것

### 공용 (`_build/parts/`)

| 파일 | 내용 |
|---|---|
| `style.css` | 디자인 토큰(`:root`) · 리셋 · 레이아웃 · 버튼 · 카드 · 헤더/GNB · 폼 · 표 · 탭 · 아코디언 · 푸터 · 반응형 3단 |
| `common.js` | 활성 메뉴 표시 · 모바일 서랍 · 등장 애니메이션 · 숫자 카운트업 · 관련사이트 select · 좌측메뉴→select · 앵커 위치 보정 · 아코디언 |
| `header.html` | 유틸바 + 로고 + 4개 GNB(2depth 드롭다운) + 햄버거 |
| `footer.html` | 정책 링크 + 주소 + 관련사이트 select + **실습본 안내 문구** |

### 페이지 5개

| 페이지 | 다루는 것 |
|---|---|
| `index.html` | 히어로 슬라이더(자동재생 · 인디케이터 · 스와이프 · 키보드), 퀵메뉴, 탭 게시판, 제품 카드, 숫자 카운트업 |
| `about.html` | breadcrumb, 좌측 세로 메뉴 + 스크롤 추적, 연혁 타임라인 + 연대 필터, CI 색상칩, SVG 약도, 항목형 표 |
| `products.html` | 분류 탭, 검색(디바운스), 정렬, 카드 그리드, `<dialog>` 상세 모달, `?cat=` 반영 |
| `notice.html` | 게시판 표, 분류 탭, 범위 지정 검색, 검색어 하이라이트, 쪽 나누기, 제목 클릭 시 본문 펼침 |
| `support.html` | FAQ 아코디언 + 검색 + 모두 펼치기, 문의 폼 유효성 검사, 개인정보 동의, 연동 select |
| `instructor.html` | 강사 프로필 카드, 학력 · 경력 · 핵심역량 · 자격 · 저서 (10장에서 추가) |

### 빌드 (`_build/build.mjs`)

`<!--@meta-->` `<!--@style-->` `<!--@body-->` `<!--@script-->` 네 구역으로 나뉜
페이지 조각을 읽어, 공용 조각과 합쳐 단일 HTML 을 뱉는다.
외부 패키지 없이 Node 표준 모듈만 쓴다.

---

## 6. 검증

`npm run build` 같은 것으로는 화면이 맞는지 알 수 없다.
**실제 브라우저(Chromium)를 띄워 확인했다.**

### 6-1. 폭별 자동 점검

5개 페이지 × 3개 폭(360 · 768 · 1440) = 15개 조합에서 매번 확인한 것

- 스크립트 오류 · 콘솔 오류 개수
- `documentElement.scrollWidth - clientWidth` — **본문이 가로로 밀렸는지**
- 글자 크기 16px 미만인 입력 필드 개수 — **iOS 확대 방지**
- 높이 40px 미만인 버튼 개수 — **터치 대상 크기**

최종 결과:

```
✓ index      360px  overflow=0  smallInput=0  smallTap=0
✓ index      768px  overflow=0  smallInput=0  smallTap=0
✓ index     1440px  overflow=0  smallInput=0  smallTap=0
✓ about      360px  overflow=0  smallInput=0  smallTap=0
   … (15개 조합 전부)
모든 조합 통과
```

### 6-2. 상호작용 점검 (실제 클릭)

```
슬라이더 transform: translateX(-100%)      ← 다음 버튼 동작
보도자료 패널 보임: true                    ← 탭 전환
양돈 카드 수: 4                             ← ?cat=pig 반영
"자돈" 검색 결과: 1                          ← 검색 필터
모달 열림: true                             ← <dialog> 상세
1쪽 글 수: 8                                ← 쪽당 개수
2쪽 이동 후 주소: ?p=2                       ← 주소 반영
"채용" 검색 결과 수: 3                       ← 게시판 검색
빈 폼 제출 → 오류 메시지: 입력하지 않았거나 형식이 맞지 않는 칸이 5개 있습니다.
올바른 폼 제출 → 클래스: form-msg is-ok
경기 대리점 수: 3                           ← 연동 select
서랍 열림: true                             ← 모바일 햄버거
상호작용 중 오류: 없음
```

### 6-3. 눈으로 확인

1440px 전체 캡처, 390px 전체 캡처, 모바일 서랍, 스티키 제출 버튼을 직접 봤다.

---

## 7. 도중에 나온 문제와 고친 내용

작업이 한 번에 된 것은 아니다. 브라우저 검증에서 잡힌 것들이다.

### 7-1. `about.html` 이 360px 에서 296px 옆으로 밀렸다

```
✗ about  360px  overflow=296
```

`.table-wrap` 으로 감쌌는데도 본문 전체가 밀렸다.

**원인:** `.sub-layout` 이 CSS Grid 인데, **grid 칸의 기본 `min-width` 는 `auto`** 다.
안쪽 표의 `min-width:640px` 만큼 칸 자체가 벌어져 버렸고,
`.table-wrap` 의 `overflow-x:auto` 는 무용지물이 됐다.

**수정:**

```css
.sub-layout > *,.grid > *{min-width:0}
```

grid/flex 안에 스크롤 영역을 넣을 때 항상 걸리는 함정이라
`강의노트.md` 에 별도 항목으로 넣었다.

### 7-2. 폼 제출 성공 메시지가 뜨자마자 사라졌다

```
올바른 폼 제출 → 클래스: form-msg      ← is-ok 가 붙어야 하는데
```

**원인:** 성공 메시지를 띄운 뒤 `form.reset()` 으로 칸을 비웠는데,
`reset()` 이 **`reset` 이벤트를 발생시킨다.** 그 처리기가
"메시지 지우기"를 하고 있어서 방금 띄운 메시지를 스스로 지웠다.

**수정:** 이번 초기화가 "제출 뒤 정리"인지 표시하고, 사용자가 직접 누른
`다시 작성` 일 때만 메시지를 지운다.

```js
clearing = true;
form.reset();
clearing = false;
...
form.addEventListener('reset', function () {
  if (!clearing) msg.className = 'form-msg';
});
```

### 7-3. 히어로 조작 버튼이 퀵메뉴 카드에 가렸다

퀵메뉴를 `margin-top:-44px` 로 히어로 위에 걸쳐 놓았는데,
슬라이더 조작 버튼이 `bottom:28px` 이라 그 44px 안에 들어가 버렸다.

**수정:** `bottom:64px` 로 올렸다.

### 7-4. 모바일에서 히어로 버튼과 조작 버튼이 겹쳤다

7-3 을 고치자 이번엔 좁은 화면에서 히어로가 짧아
`상담 신청` 버튼과 조작 버튼이 물렸다. 사각형을 실제로 계산해 확인했다.

**수정:** 900px 이하에서 히어로에 아래 여백을 준다.

```css
.hero__slide{min-height:460px; padding-bottom:112px}
```

고친 뒤 다시 계산: `히어로 CTA ↔ 조작버튼 겹침: false`

### 7-5. 스크립트가 꺼지면 본문 절반이 안 보였다

등장 애니메이션 대상(`.reveal`)은 처음에 `opacity:0` 이다.
JS 가 안 돌면 **영원히 안 보인다.**

**수정:** `<noscript>` 로 되돌린다.

```html
<noscript><style>
.reveal{opacity:1 !important; transform:none !important}
</style></noscript>
```

### 7-6. 검사 자체가 틀렸던 것 하나

`support.html` 1440px 에서 "16px 미만 입력 필드 1개"가 잡혔다.
확인해 보니 **체크박스**였다. 체크박스는 글자를 표시하지 않으므로
`font-size` 가 작아도 iOS 확대와 무관하다.

화면을 고치는 대신 **검사에서 `checkbox` · `radio` · `hidden` 을 제외**했다.
증상을 없애려고 멀쩡한 화면을 건드리지 않은 경우다.

---

## 8. 남은 것 / 하지 않은 것

- **원본 사이트의 실제 화면은 반영되지 않았다.** 접근이 막혀 있었다 (2장).
  강의실 PC 에서 원본을 열 수 있다면 2장 마지막의 순서대로 맞춰 갈 수 있다.
- 서버 · 데이터베이스는 없다. 문의 폼은 값을 콘솔에 찍고 끝난다.
  실습 단계상 의도한 것이며, 화면에도 그렇게 적어 두었다.
- 이미지 파일이 하나도 없다. 폐쇄망을 위해 일부러 뺐다.
  사진을 넣고 싶다면 `.prod__thumb` · `.greet__photo` 자리를
  `<img>` 로 바꾸면 된다 (`강의노트.md` 심화 과제).
- 접근성은 기본선까지만 했다 — 시맨틱 태그, `aria-*`, 키보드 조작,
  포커스 표시, `prefers-reduced-motion`. 스크린리더 실제 테스트는 안 했다.

---

## 9. 파일 한눈에

```
농협/
├─ index.html          44.4 KB
├─ about.html          39.0 KB
├─ products.html       40.4 KB
├─ notice.html         41.2 KB
├─ support.html        47.4 KB      ← 합계 약 212 KB, 그 밖에 필요한 파일 없음
├─ _build/  parts/{style.css, common.js, header.html, footer.html}
│           pages/{index,about,products,notice,support}.html
│           build.mjs
├─ docs/    작업기록.md (이 문서), 강의노트.md
├─ setup-local.ps1
├─ setup-local.bat
└─ README.md
```

---

## 10. 추가 작업 (2026-08-20 오후) — 강사 소개 페이지와 배포

### 10-1. 요청

1. `D:\data\dev\nonghyupsaryo-main` 의 소스를 이 사이트에 그대로 옮길 것
2. 강사 이력은 첨부한 **PDF 이력서**를 참조해 바꿀 것
3. 반응형으로 만들 것
4. 검증하고 **승인을 받은 뒤** 커밋 · 푸시 · 빌드 · 배포할 것

### 10-2. 로컬 소스에 다시 막혔다 — 이번엔 원인이 분명하다

`D:\data\dev\nonghyupsaryo-main` 을 찾아 달라는 요청을 받고 파일시스템 전체를 뒤졌다.

```
$ cat /container_info.json
{ "container_name": "…--claude_code_remote--59bb1d" }

$ mount
/dev/vda on /        ext4        ← 이 컨테이너 자신의 디스크
/dev/vdb /dev/vdc …  squashfs    ← 읽기 전용 시스템 이미지
                                   (호스트 PC 연결도, 네트워크 공유도 없다)

$ for p in /mnt/d /d /media/d /cygdrive/d /host/d; do … done
전부 없음

$ find / -xdev \( -iname "*nonghyup*" -o -iname "*농협*" \)
(이 저장소의 농협/ 폴더 외에 없음)
```

3장에 적은 것과 같은 사정이지만, 이번엔 이유가 더 분명해졌다.
**이 세션은 사용자 PC 가 아니라 클라우드 컨테이너에서 돌아간다.**
`D:` 드라이브는 사용자 PC 안에만 있고, 여기까지 파일이 오려면 대화에 올려야 한다.

→ 소스는 **ZIP 첨부**로 받기로 했다. 받는 즉시 이어서 옮긴다.
   그때까지 원본 복제는 보류다.

### 10-3. 소스 없이도 되는 것부터 했다 — 강사 소개 페이지

첨부된 PDF 이력서(A4 3쪽)는 텍스트를 뽑을 수 있었다.
다만 이 환경의 PDF 도구가 둘 다 망가져 있어 한 단계가 더 필요했다.

- `pypdf` → `cryptography` 의 러스트 바인딩이 깨져 `PanicException`
- 내장 PDF 리더 → `pdftoppm` 없음

```bash
apt-get update -q && apt-get install -y poppler-utils
pdftotext -layout <파일> -
```

`apt-get update` 를 먼저 돌려야 했다. 색인이 낡아 처음엔 404 가 났다.

읽어 낸 내용으로 **`instructor.html`** 을 새로 만들었다.

| 구역 | 내용 |
|---|---|
| 프로필 | 이름 · 영문 · 직함 3개 · 자기소개 · 연락처 |
| 학력사항 | 5건 (연도가 오른쪽에 붙는 줄 배치) |
| 주요 경력 | 7건 |
| 핵심역량 | 두 분류 17건 (강의 이력) |
| 자격사항 | 4개 그룹, 카드 2×2 |
| 저서 | 2권 + VISION 띠 |

공용 헤더(`_build/parts/header.html`)에 `강사 소개` 메뉴와 2depth 6개를 넣었다.
좌측 세로 메뉴는 `about.html` 과 같은 방식으로 스크롤 위치를 따라 활성 표시된다.

**연락처 주의:** 이력서에 있는 휴대전화 번호를 프로필에 그대로 실었다.
공개 사이트에 올릴 때 뺄지 말지는 사람이 정할 일이라 그대로 두고 짚어만 두었다.

### 10-4. 검증

기존 검사에 `instructor` 를 넣어 다시 돌렸다.

```
✓ instructor  360px  overflow=0  smallInput=0  smallTap=0
✓ instructor  768px  overflow=0  smallInput=0  smallTap=0
✓ instructor 1440px  overflow=0  smallInput=0  smallTap=0
…
18개 조합 전부 통과 (6쪽 × 3폭)
```

1440px · 390px 전체 캡처도 눈으로 확인했다. 이번엔 새로 고칠 것이 없었다 —
공용 스타일이 이미 6장에서 다듬어져 있었기 때문이다.
**새 페이지가 기존 규칙을 그냥 물려받는 것**이 디자인 시스템을 따로 둔 이유다.

### 10-5. 배포 — 워크플로에 자리가 없었다

"배포해 달라"는 말을 듣고 `.github/workflows/deploy.yml` 을 열어 보니
**`농협/` 은 배포 대상이 아니었다.**

```yaml
on:
  push:
    branches: [main]
    paths:
      - 'web/**'          ← 농협/ 이 바뀌어도 워크플로가 안 돈다
…
      - uses: actions/upload-pages-artifact@v3
        with:
          path: web/dist  ← 게시되는 것은 LMS 앱뿐
```

즉 브랜치를 `main` 에 넣어도 아무 일도 일어나지 않는다. 두 곳을 고쳤다.

1. `paths` 에 `'농협/**'` 추가 — 이 폴더가 바뀌어도 워크플로가 돌게
2. 빌드 뒤 `농협/*.html` 을 `web/dist/nonghyup/` 으로 복사

**게시 경로를 `nonghyup` 으로 바꾼 이유:** 폴더 이름이 한글이면 주소가
`%EB%86%8D%ED%98%91` 로 인코딩되어 링크를 읽기 어렵다.
저장소 안 폴더는 `농협/` 그대로 두고 **내보낼 때만** 이름을 바꾼다.

LMS 앱(`web/`) 은 한 줄도 건드리지 않았다. 산출물에 폴더 하나가 더 붙을 뿐이다.

주소: `https://lms.miraejob.co.kr/nonghyup/`

### 10-6. 아직 안 된 것

- **원본(`nonghyupsaryo-main`) 복제는 보류다.** ZIP 을 받는 즉시 진행한다.
  지금 올라간 것은 6장까지 만든 실습본 + 강사 소개 페이지다.
- 강사 사진이 없다. `.cv-photo` 자리에 `<img>` 를 넣으면 된다.

---

## 11. 저장소 분리 (2026-08-20 저녁) — `jungaistar/nonghyup`

### 11-1. 왜 나눴나

여기까지 이 자료는 상호평가 시스템(`jungaistar/lms`) 안의 `농협/` 폴더에 있었다.
성격이 전혀 다른 두 가지가 한 저장소에 섞여 있는 상태였다.

| | `lms` | 이 자료 |
|---|---|---|
| 무엇 | 학생 성적을 다루는 운영 시스템 | 강의용 실습 자료 |
| 구성 | React + Supabase + RLS | 정적 HTML 6개 |
| 배포 | 학생이 쓰는 사이트 | 강의 확인용 |
| 고치는 이유 | 학사 일정 | 강의 준비 |

강의 자료를 고칠 때마다 운영 시스템 저장소에 커밋이 쌓이고,
배포 워크플로도 남의 집에 얹혀 있었다. 그래서 따로 냈다.

### 11-2. 저장소를 자동으로 만들지 못했다

```
POST https://api.github.com/user/repos
→ 403 Resource not accessible by integration
```

이 세션의 GitHub 앱 토큰은 **이미 연결된 저장소에만 접근**할 수 있고
새 저장소를 만들 권한이 없다. 설계상 그런 것이라 우회하지 않았다.

→ **빈 저장소를 만드는 것만 사람이 누르고**, 내용은 준비해 둔 것을 그대로 밀어 넣었다.

### 11-3. 옮기면서 바꾼 것

폴더를 통째로 복사한 게 아니라 몇 군데를 저장소 기준으로 고쳤다.

| 대상 | 전 | 후 |
|---|---|---|
| 파일 위치 | `농협/index.html` | `index.html` (저장소 뿌리) |
| 로컬 배치 경로 | `D:\data\dev\농협` | `D:\data\dev\nonghyup` |
| 배포 워크플로 | `lms` 것에 얹혀 있었음 | 이 저장소 전용 |
| 게시 주소 | `lms.miraejob.co.kr/nonghyup/` | `jungaistar.github.io/nonghyup/` |

**저장소 이름을 `농협` 이 아니라 `nonghyup` 으로 한 이유:**
저장소 이름이 한글이면 주소가 `%EB%86%8D%ED%98%91` 로 인코딩되어 읽기 어렵고,
`<저장소명>.miraejob.co.kr` 규칙에 태우면 아예 유효한 호스트 이름이 되지 않는다.
게시 경로를 `nonghyup` 으로 정한 것과 같은 이유다.

### 11-4. 배포 워크플로를 새로 짰다

`lms` 것은 `npm ci && npm run build` 로 React 앱을 만든 다음
그 산출물에 이 자료를 얹는 구조였다. 여기서는 빌드할 것이 없다.

```yaml
      - name: 게시할 파일만 모은다
        run: |
          mkdir -p _site
          cp *.html _site/
```

`_build/` · `docs/` · 배치 스크립트는 웹에 올리지 않는다. 만드는 재료일 뿐이다.

**한 가지를 더 넣었다 — 원본과 결과물이 어긋나는 것을 막는 검사.**

```yaml
      - name: 커밋된 HTML 이 _build/ 와 일치하는지 확인
        run: |
          node _build/build.mjs
          if ! git diff --quiet -- '*.html'; then
            echo "::error::커밋된 HTML 이 _build/ 의 원본과 다릅니다."
            exit 1
          fi
```

인라인 방식의 가장 큰 위험은 **`_build/parts/style.css` 만 고치고 다시 만들지
않은 채 커밋하는 것**이다. 그러면 저장소의 HTML 은 옛 스타일을 그대로 들고 있고,
아무도 눈치채지 못한 채 배포된다. CI 가 직접 다시 만들어 비교하게 했다.

### 11-5. `lms` 쪽에 남은 것

`jungaistar/lms` 의 `농협/` 폴더와 `deploy.yml` 의 복사 단계는 **그대로 두었다.**
지우는 것은 되돌리기 어려운 쪽이라 사람이 정할 일로 남겼다.

지우기로 하면 두 가지를 같이 해야 한다.

1. `농협/` 폴더 삭제
2. `.github/workflows/deploy.yml` 에서 `'농협/**'` paths 항목과
   `web/dist/nonghyup/` 복사 단계 되돌리기

둘 중 하나만 하면 워크플로가 없는 폴더를 복사하려다 실패한다.
