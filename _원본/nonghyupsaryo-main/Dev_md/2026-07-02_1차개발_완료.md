# 농협사료 학습사이트 · 1차 개발 완료 보고

- **완료일**: 2026-07-02
- **사이트**: https://nonghyupsaryo.dreamitbiz.com (라이브)
- **리포**: aebonlee/nonghyupsaryo
- **과정**: 생성형 AI 기반 사무관리 업무혁신 실무 (농협사료 임직원 · 1일 8차시 · 08:00~17:00)
- **상태**: ✅ 1차 개발 종료 · 전 기능 라이브 · Actions 자동배포 정상

---

## 1. 개요

농협사료 사무관리직 임직원을 위한 「생성형 AI 기반 사무관리 업무혁신 실무」 1일 8차시 과정의
학습사이트를 처음부터 끝까지 구축·배포했다. 기존 **bulid**(건설기계) 프로젝트를 베이스로
2권×6일 구조를 **단일 과정 × 8차시** 로 재편하고, 농협(NH) 아이덴티티로 리브랜딩했다.

## 2. 기술 스택

- React 18 · Vite 5 · React Router 6 (HashRouter)
- Tailwind CSS (Pretendard, Font Awesome 6)
- Supabase — 구글·카카오 OAuth, 학습 진도 동기화(`nonghyupsaryo_progress`, RLS)
- GitHub Actions → GitHub Pages 자동배포 (커스텀 도메인, base `'/'`)

## 3. 작업 타임라인 (커밋 단위)

| # | 커밋 | 핵심 내용 |
|---|------|-----------|
| 1 | 초기 개발 | bulid 복사 → 단일 8차시 재편, NH 그린(#12522e)+골드(#fec20d) 리브랜딩, content.json 8차시 신규, tools/labs/appendix/promptLab 사무관리 재작성, 컴포넌트 단일과정 적응 |
| 2 | 콘텐츠 확충 + 메뉴 순서 | 8차시 본문을 아카데믹+기술+초등학생 눈높이 3층 설명으로 대폭 확충(~407블록), 헤더 교육일정↔8차시 학습 순서 교체 |
| 3 | 복사 버튼 추가 | 8차시 프롬프트 박스 우측 상단 복사 버튼 |
| 4 | 복사 버튼 통일 | 공용 `CopyButton` 컴포넌트로 5개 화면 통일 + ToolPage 누락분 보완 |
| 5 | 로그인/진도 수정 | 진도 upsert/delete try/catch 방어, `nonghyupsaryo_progress` 테이블 SQL 추가 |
| 6 | 헤더 미세조정 | 데스크톱 상단 메뉴 자간(tracking-tight)·간격(gap/구분선) 조정 |
| 7 | 모바일 최적화 | overflow-x 차단, 본문 텍스트·히어로 반응형, 통계 2×2 그리드 |

## 4. 브랜딩 (농협 CI)

- 색상: **딥 그린 #12522e + 골드 #fec20d** — 골드값은 제공받은 공식 CI 파일에서 실제 색 추출
- 로고: 제공된 `시그니춰.ai`(PostScript)를 **Ghostscript 렌더 → PIL로 심볼 자동 크롭** →
  투명 PNG(`public/nh-symbol.png`)·파비콘(`public/favicon.png`) 생성 → 헤더·로그인모달에 적용

## 5. 콘텐츠 (8차시, ~407블록)

프롬프트 → 사무업무별 활용 → 시장분석 → 전략기획 → 보고서 자동화 → 데이터 분석 → 의사결정 → 종합실습.
- 블록 분포: 본문 137 · 표 30 · 프롬프트박스 31(복붙용) · 실습 29 · 팁 31 · 유의 20 · 사례 11 · 산출물 18
- LLM 원리(다음단어 예측·토큰·컨텍스트·환각·온도)를 끝말잇기 등 비유로 설명
- 무료 도구 6종(ChatGPT·Claude·Gemini·Perplexity·Google Sheets·Docs)만 사용
- 부가: 실습 따라하기(21종)·업무별 프롬프트 부록(21종)·프롬프트 SCORE 학습·AI 도구 가이드

## 6. 배포 이슈 & 해결

- **빈 화면(중요)**: 신규 Pages가 `legacy`로 시작 → 리포 루트의 소스 `index.html`(→`/src/main.jsx`)을 서빙.
  → `gh api -X PUT .../pages -f build_type=workflow` 전환 **후 워크플로 재실행**으로 dist 아티팩트 활성화. 해결.
- **로그인 문제**: 로그인 자체는 정상(카카오 세션 확인). 원인은 `nonghyupsaryo_progress` **테이블 부재**로 진도 저장 실패.
  → 코드 방어처리(try/catch) + 테이블 수동 생성(`supabase/nonghyupsaryo_progress.sql`)으로 해결. 테이블 200 확인.

## 7. 검증

- `npm run build` 성공(106 modules), 모든 블록 타입 렌더러 지원 확인
- 라이브 헬스체크: 페이지·번들·심볼·파비콘·OG 전부 200
- 잔재 스캔: 건설기계·굴삭·Genspark·제1권 0건
- 진도 테이블 REST 200(RLS) 확인

## 8. 저장소 구조

- `src/data/` — content.json(8차시)·dayplans·tools·labs·appendix·promptLab·about
- `src/pages/`·`src/components/` — 페이지·UI(블록 렌더링, 공용 CopyButton)
- `public/nh-symbol.png`·`favicon.png` — 농협 CI 추출 에셋
- `supabase/nonghyupsaryo_progress.sql` — 진도 동기화 테이블(RLS)
- `reference/` — 원본 교육 제안서 docx·농협 CI(ai)
- `Dev_md/` — 개발일지

## 9. 마감 이후 추가 반영 (2차 검토 항목 처리)

- **OG 이미지 재제작(canvas)**: 기존 bulid(건설기계) 이미지 → 농협 그린+골드·심볼·8차시·도구칩 반영.
  `scripts/gen-og.mjs`(node-canvas)로 생성. 카카오/페북 이미지 캐시 회피 위해 **파일명 `og.png`로 교체**(옛 `og-image.png`는 옛 캐시 유지) + 메타 갱신. 공유 미리보기 정상 확인.
- **6차시 실습 샘플 데이터 첨부**: `public/samples/` 가상 CSV 3종(사료판매·거래처주문·고객문의, UTF-8 BOM).
  `BlockRenderer`에 `download` 블록 타입 신설(한글 파일명 인코딩) → 6차시 "Sheets 표 분석" 섹션에 다운로드 카드. `scripts/gen-samples.py` 생성기.

## 10. 강의 일정 확정 반영 (2026-07-02)

- **일시 확정**: 2026. 8. 20.(목) 08:00~17:00 (점심 1시간 제외, 총 8시간)
- **장소**: 농협사료 5층 중회의실 (서울시 강동구 올림픽로 528)
- 반영 위치: 교육일정 페이지 헤더(일시·장소 카드), Home 히어로 일시·장소 라인. "8월 중 협의" 문구 제거.

## 11. 미결

- 모바일 실기기 QA 후 미세조정(현재 반응형 코드 기반 최적화 완료)

— 1차 개발 최종 종료 (2026-07-02). 전 기능 라이브, 전 커밋 푸시·배포 완료.

— 1차 개발 종료. 전 기능 라이브.
